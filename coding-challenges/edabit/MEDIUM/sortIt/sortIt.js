//Edabit solution 1

const sortIt = (arr) => arr.sort((a, b) => (a[0] || a) - (b[0] || b));

//Edabit solution 2
const sortIt2 = (arr) =>
  arr.sort((a, b) => a - (typeof b === "object" ? b[0] : b));
function sortIt(arr) {
  for (let i = 0; i < array.length; i++) {
      const element = array[i];
      if(element.isInteger())
  }
}
